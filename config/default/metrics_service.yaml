apiVersion: v1
kind: Service
metadata:
  labels:
    control-plane: session-controller
    app.kubernetes.io/name: codespace-operator
    app.kubernetes.io/managed-by: kustomize
  name: session-controller-metrics-service
  namespace: system
spec:
  ports:
  - name: https
    port: 8443
    protocol: TCP
    targetPort: 8443
  selector:
    control-plane: session-controller
    app.kubernetes.io/name: codespace-operator
